// Cargo.toml dependencies (suggested)
// [dependencies]
// anyhow = "1.0"         # optional for error handling
// serde = { version = "1.0", features = ["derive"] }  # if parsing JSON later
// log = "0.4"            # optional for logging
